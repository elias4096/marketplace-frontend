server {
    listen 80;
    server_name localhost;  # Adjust if deploying to a different domain

    location / {
        root /usr/share/nginx/html;  # Path to your React build files
        try_files $uri /index.html;   # Serve index.html for unmatched routes
    }
}
